{
  "name": "admin",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test src/*.test.mjs",
    "clean": "rm -rf dist"
  }
}

{
  "name": "prepaid-shield",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:mobile": "pnpm --filter mobile start",
    "demo:reset": "DATABASE_URL=\"file:./dev.db\" pnpm --filter api exec prisma db push && DATABASE_URL=\"file:./dev.db\" pnpm --filter api seed",
    "demo:api": "DEMO_MODE=true DATABASE_URL=\"file:./dev.db\" pnpm --filter api dev",
    "demo:mobile": "pnpm --filter mobile start",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  },
  "packageManager": "pnpm@10.8.1",
  "engines": {
    "node": ">=20"
  }
}
